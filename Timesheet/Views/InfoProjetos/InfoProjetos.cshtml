@{
    //ViewBag.Title = System.Configuration.ConfigurationManager.AppSettings["TITLEAPP"].ToString();
    Layout = "~/Views/Shared/_Principal.cshtml";
    //var relativePath = System.Configuration.ConfigurationManager.AppSettings["RELATIVEPATH"].ToString();

    List<Apassos.Models.Project> listProject = (List<Apassos.Models.Project>)HttpContext.Current.Session["LISTA_PROJETOS"];

    var relativePath = System.Configuration.ConfigurationManager.AppSettings["RELATIVEPATH"].ToString();
    string idProject = string.Empty;
    string projeto = (string)HttpContext.Current.Session["PROJETO"];
}

<script src="@Url.Content("~/Scripts/jquery.maskedinput-1.3.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/InfoProjetos/InfoProjetos.js")" type="text/javascript"></script>



<table id="tabelaperiodos" style="width:100%;border-spacing:5px;">


    @*@foreach (var project in listProject)
    {
        <tr class="tritem">

            <td>
                @if (!(@project.PROJECTID == 0))
                {
                    @project.PROJECTID.ToString().Replace(';', ' ');
                }
            </td>
            <td>
                @if (!(@project.NAME == null))
                {
                    @project.NAME.ToString().Replace(';', ' ');
                }
            </td>
            <td>
                @if (!(@project.DESCRIPTION == null))
                {
                    @project.DESCRIPTION.ToString().Replace(';', ' ');onclick="ExportToExcel(@report)"onclick="Procurar(@idProject)"
                }

            </td>

        </tr>
    }*@


    <tr class="trheader">

        <th>
            ID
        </th>
        <th>
            Nome Projeto
        </th>
        <th>Descrição</th>
        <th>Cliente</th>

    </tr>

    @foreach (var project in listProject)
        {
            <tr class="tritem">

                <td>
                    @if (!(@project.PROJECTID == 0))
                    {
                        @project.PROJECTID.ToString().Replace(';', ' ');
                    }
                </td>
                <td>
                    @if (!(@project.NAME == null))
                    {
                        @project.NAME.ToString().Replace(';', ' ');
                    }
                </td>
                <td>
                    @if (!(@project.DESCRIPTION == null))
                    {
                        @project.DESCRIPTION.ToString().Replace(';', ' ');
                    }

                </td>

                <td>
                    @if (!(@project.Partner.FIRSTNAME == null))
                    {
                        @project.Partner.FIRSTNAME.ToString().Replace(';', ' ');
                    }

                </td>

            </tr>
        }

</table>


